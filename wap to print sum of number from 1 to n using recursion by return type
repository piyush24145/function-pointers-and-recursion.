#include <stdio.h>
int add(int n){
    if(n<=0 )      return 0;
    int recans=n+add(n-1);
    return recans;
    
}


int main(){
    int n;
    printf("enter n :");
    scanf("%d",&n);
    int sum=add(n);
    printf("%d",sum);
    return 0;
}
