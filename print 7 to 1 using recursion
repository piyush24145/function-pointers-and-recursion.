#include <stdio.h>
void decending(int n){  
    if(n==0)return ;
    printf("%d\n",n);
        decending(n-1);
        return;
}
int main (){
    int n;        //5
    printf("enter n:");
    scanf("%d",&n);
    decending(n); // call function 
    return 0;
}
